import {Component, PropTypes} from 'react';

export default class extends Component {

    static propTypes = {
        onClick: PropTypes.func
    };

    render() {
        return (
            <div></div>
        );
    }
}
